/**
 * Portuguese text for the application shell.
 *
 * Components import their text from here rather than inlining it, so that
 * `src/copy/` stays the one place to review everything the user reads — see
 * ADR 0004.
 */

export const APP_TITLE = 'Calculadora de porção diária de ração';

/** Placeholder while the form does not exist. The UI branch replaces it. */
export const SHELL_NOTICE = 'Em construção. O motor de cálculo vive em src/domain/.';
